module.exports = [
"[project]/.next-internal/server/app/api/uploadMenu/route/actions.js [app-rsc] (server actions loader, ecmascript)", ((__turbopack_context__, module, exports) => {

}),
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[externals]/perf_hooks [external] (perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("perf_hooks", () => require("perf_hooks"));

module.exports = mod;
}),
"[externals]/node:perf_hooks [external] (node:perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:perf_hooks", () => require("node:perf_hooks"));

module.exports = mod;
}),
"[externals]/express [external] (express, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("express", () => require("express"));

module.exports = mod;
}),
"[externals]/fs/promises [external] (fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs/promises", () => require("fs/promises"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/process [external] (process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("process", () => require("process"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:async_hooks [external] (node:async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:async_hooks", () => require("node:async_hooks"));

module.exports = mod;
}),
"[externals]/async_hooks [external] (async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("async_hooks", () => require("async_hooks"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[externals]/child_process [external] (child_process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("child_process", () => require("child_process"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/require-in-the-middle [external] (require-in-the-middle, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("require-in-the-middle", () => require("require-in-the-middle"));

module.exports = mod;
}),
"[externals]/import-in-the-middle [external] (import-in-the-middle, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("import-in-the-middle", () => require("import-in-the-middle"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/tls [external] (tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tls", () => require("tls"));

module.exports = mod;
}),
"[externals]/http2 [external] (http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http2", () => require("http2"));

module.exports = mod;
}),
"[externals]/dns [external] (dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("dns", () => require("dns"));

module.exports = mod;
}),
"[externals]/dgram [external] (dgram, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("dgram", () => require("dgram"));

module.exports = mod;
}),
"[externals]/assert [external] (assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("assert", () => require("assert"));

module.exports = mod;
}),
"[project]/src/ai/genkit.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ai",
    ()=>ai
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/genkit/lib/index.mjs [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$genkit$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/genkit/lib/genkit.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$genkit$2d$ai$2f$googleai$2f$lib$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@genkit-ai/googleai/lib/index.mjs [app-route] (ecmascript) <locals>");
;
;
const ai = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$genkit$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["genkit"])({
    plugins: [
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$genkit$2d$ai$2f$googleai$2f$lib$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["googleAI"])()
    ],
    model: 'googleai/gemini-1.5-flash'
});
}),
"[project]/src/lib/inventoryService.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Inventory Service
// Provides functions to manage inventory in localStorage
__turbopack_context__.s([
    "addIngredientToInventoryIfNotExists",
    ()=>addIngredientToInventoryIfNotExists,
    "addInventoryItem",
    ()=>addInventoryItem,
    "addOrUpdateIngredientInInventory",
    ()=>addOrUpdateIngredientInInventory,
    "getInventory",
    ()=>getInventory,
    "getInventoryAlerts",
    ()=>getInventoryAlerts,
    "getInventoryAlertsDetailed",
    ()=>getInventoryAlertsDetailed,
    "recordIngredientUsage",
    ()=>recordIngredientUsage,
    "removeInventoryItem",
    ()=>removeInventoryItem,
    "saveInventory",
    ()=>saveInventory,
    "updateInventoryAlerts",
    ()=>updateInventoryAlerts,
    "updateInventoryItem",
    ()=>updateInventoryItem
]);
const INVENTORY_KEY_PREFIX = 'inventory_';
function getInventory(userId) {
    if ("TURBOPACK compile-time truthy", 1) return [];
    //TURBOPACK unreachable
    ;
    const data = undefined;
}
function addInventoryItem(userId, item) {
    if ("TURBOPACK compile-time truthy", 1) return null;
    //TURBOPACK unreachable
    ;
    const inventory = undefined;
    const newItem = undefined;
}
function updateInventoryItem(userId, item) {
    if ("TURBOPACK compile-time truthy", 1) return false;
    //TURBOPACK unreachable
    ;
    const inventory = undefined;
    const idx = undefined;
}
function removeInventoryItem(userId, itemId) {
    if ("TURBOPACK compile-time truthy", 1) return false;
    //TURBOPACK unreachable
    ;
    const inventory = undefined;
    const newInventory = undefined;
}
function addOrUpdateIngredientInInventory(userId, ingredient) {
    const inventory = getInventory(userId);
    const idx = inventory.findIndex((i)=>i.name.toLowerCase() === ingredient.name.toLowerCase());
    if (idx !== -1) {
        inventory[idx] = {
            ...inventory[idx],
            ...ingredient,
            quantityUsed: inventory[idx].quantityUsed || 0,
            totalUsed: inventory[idx].totalUsed || 0
        };
    } else {
        inventory.push({
            ...ingredient,
            id: ingredient.id || Date.now().toString(),
            quantityUsed: ingredient.quantityUsed || 0,
            totalUsed: ingredient.totalUsed || 0
        });
    }
    localStorage.setItem(INVENTORY_KEY_PREFIX + userId, JSON.stringify(inventory));
    return ingredient;
}
function addIngredientToInventoryIfNotExists(userId, ingredient) {
    const inventory = getInventory(userId);
    const exists = inventory.some((i)=>i.name.toLowerCase() === ingredient.name.toLowerCase());
    if (!exists) {
        addInventoryItem(userId, ingredient);
        return true;
    }
    return false;
}
function getInventoryAlerts(userId) {
    const inventory = getInventory(userId);
    return inventory.filter((i)=>i.lowStockThreshold && i.quantity <= i.lowStockThreshold).map((i)=>`Low stock: ${i.name} (${i.quantity} ${i.unit})`);
}
function updateInventoryAlerts(userId) {
    if ("TURBOPACK compile-time truthy", 1) return;
    //TURBOPACK unreachable
    ;
}
function saveInventory(userId, inventory) {
    localStorage.setItem(INVENTORY_KEY_PREFIX + userId, JSON.stringify(inventory));
}
function getInventoryAlertsDetailed(userId) {
    const inventory = getInventory(userId);
    return inventory.filter((i)=>i.lowStockThreshold !== undefined && i.quantity <= (i.lowStockThreshold ?? 0)).map((i)=>({
            itemId: i.id,
            itemName: i.name,
            quantity: i.quantity,
            unit: i.unit,
            message: `Low stock: ${i.name} (${i.quantity} ${i.unit})`
        }));
}
function recordIngredientUsage(userId, dish, quantity) {
// No-op placeholder
}
}),
"[project]/src/lib/menuService.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Menu Service
// Provides functions to manage menu items in localStorage
__turbopack_context__.s([
    "addDish",
    ()=>addDish,
    "addDishToMenu",
    ()=>addDishToMenu,
    "getDishes",
    ()=>getDishes,
    "removeDish",
    ()=>removeDish,
    "saveDishes",
    ()=>saveDishes,
    "updateDish",
    ()=>updateDish
]);
const MENU_KEY_PREFIX = 'menu_';
function getDishes(userId) {
    if ("TURBOPACK compile-time truthy", 1) return [];
    //TURBOPACK unreachable
    ;
    const data = undefined;
}
function saveDishes(userId, dishes) {
    localStorage.setItem(MENU_KEY_PREFIX + userId, JSON.stringify(dishes));
}
function addDish(userId, dish) {
    if ("TURBOPACK compile-time truthy", 1) return null;
    //TURBOPACK unreachable
    ;
    const dishes = undefined;
    const newDish = undefined;
}
function updateDish(userId, dish) {
    if ("TURBOPACK compile-time truthy", 1) return false;
    //TURBOPACK unreachable
    ;
    const dishes = undefined;
    const idx = undefined;
}
function removeDish(userId, dishId) {
    if ("TURBOPACK compile-time truthy", 1) return false;
    //TURBOPACK unreachable
    ;
    const dishes = undefined;
    const newDishes = undefined;
}
const addDishToMenu = addDish;
}),
"[project]/src/ai/flows/ingredient-types.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GenerateIngredientsListInputSchema",
    ()=>GenerateIngredientsListInputSchema,
    "GenerateIngredientsListOutputSchema",
    ()=>GenerateIngredientsListOutputSchema,
    "IngredientSchema",
    ()=>IngredientSchema,
    "SuggestExpiryDateInputSchema",
    ()=>SuggestExpiryDateInputSchema,
    "SuggestExpiryDateOutputSchema",
    ()=>SuggestExpiryDateOutputSchema
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/genkit/lib/index.mjs [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/genkit/lib/common.js [app-route] (ecmascript)");
;
const GenerateIngredientsListInputSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].object({
    dishName: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('The name of the dish to generate ingredients for.'),
    numberOfServings: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].number().describe('The number of servings the ingredients should be for.')
});
const IngredientSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].object({
    name: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('The name of the ingredient.'),
    quantity: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].number().describe('The quantity of the ingredient (numeric value).'),
    unit: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('The unit for the quantity (e.g., "g", "ml", "pcs", "kg").')
});
const GenerateIngredientsListOutputSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].object({
    ingredients: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].array(IngredientSchema).describe('A list of ingredients with their names, quantities, and units.')
});
const SuggestExpiryDateInputSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].object({
    productName: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('The name of the product.'),
    productCategory: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().optional().describe('The category of the product (e.g., "Tea", "Spices", "Sweets").'),
    productWeight: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().optional().describe('The weight/unit of the product (e.g., "500g", "1000g", "20 pkt").'),
    manufacturingDate: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().optional().describe('The manufacturing date if available.')
});
const SuggestExpiryDateOutputSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].object({
    suggestedExpiryDate: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('The suggested expiry date in YYYY-MM-DD format.'),
    shelfLifeDays: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].number().describe('The estimated shelf life in days.'),
    storageRecommendation: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('Storage recommendation for the product.'),
    confidence: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$genkit$2f$lib$2f$common$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["z"].string().describe('Confidence level of the suggestion (High, Medium, Low).')
});
}),
"[project]/src/ai/flows/generate-ingredients-list.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/* __next_internal_action_entry_do_not_use__ [{"405410787db2d797b9d5d69a0631fed08a4836012e":"generateIngredientsList"},"",""] */ __turbopack_context__.s([
    "generateIngredientsList",
    ()=>generateIngredientsList
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$webpack$2f$loaders$2f$next$2d$flight$2d$loader$2f$server$2d$reference$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/build/webpack/loaders/next-flight-loader/server-reference.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$genkit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/ai/genkit.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$ingredient$2d$types$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/ai/flows/ingredient-types.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$webpack$2f$loaders$2f$next$2d$flight$2d$loader$2f$action$2d$validate$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/build/webpack/loaders/next-flight-loader/action-validate.js [app-route] (ecmascript)");
;
;
;
const prompt = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$genkit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ai"].definePrompt({
    name: 'generateIngredientsListPrompt',
    input: {
        schema: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$ingredient$2d$types$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GenerateIngredientsListInputSchema"]
    },
    output: {
        schema: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$ingredient$2d$types$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GenerateIngredientsListOutputSchema"]
    },
    prompt: `You are a chef. Generate a list of ingredients needed for the dish "{{dishName}}" for {{numberOfServings}} servings.

IMPORTANT:
- The language of the ingredient names and units must always match the language of the dish name. If the dish name is in German, all ingredient names and units must be in German. If the dish name is in English, use English. Do not translate or mix languages.
- Always include the main protein or key ingredient (e.g., if the dish is "Palak Chicken" or "Chicken Palak", always include "Chicken" as an ingredient).

For each ingredient, provide its name, quantity (as a number), and unit (e.g., "g", "ml", "pcs", "kg").
Never return a quantity of 0. If unsure, estimate a typical amount for one serving based on common recipes or chef experience.
Return the output as a JSON object with a single key "ingredients".
The "ingredients" key should have a value of an array of objects, where each object has "name" (string), "quantity" (number), and "unit" (string) fields.
Be as accurate as possible.

Example (English):
{
  "ingredients": [
    { "name": "Chicken Breast", "quantity": 200, "unit": "g" },
    { "name": "Spinach", "quantity": 150, "unit": "g" },
    { "name": "Onion", "quantity": 50, "unit": "g" }
  ]
}

Example (German):
{
  "ingredients": [
    { "name": "Hähnchenbrust", "quantity": 200, "unit": "g" },
    { "name": "Spinat", "quantity": 150, "unit": "g" },
    { "name": "Zwiebel", "quantity": 50, "unit": "g" }
  ]
}

Ensure the output strictly follows this JSON format.`
});
const generateIngredientsListFlow = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$genkit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ai"].defineFlow({
    name: 'generateIngredientsListFlow',
    inputSchema: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$ingredient$2d$types$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GenerateIngredientsListInputSchema"],
    outputSchema: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$ingredient$2d$types$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GenerateIngredientsListOutputSchema"]
}, async (input)=>{
    const { output } = await prompt(input);
    if (!output) {
        throw new Error('AI did not return an output.');
    }
    return output;
});
async function generateIngredientsList(input) {
    return generateIngredientsListFlow(input);
}
;
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$webpack$2f$loaders$2f$next$2d$flight$2d$loader$2f$action$2d$validate$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ensureServerEntryExports"])([
    generateIngredientsList
]);
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$webpack$2f$loaders$2f$next$2d$flight$2d$loader$2f$server$2d$reference$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["registerServerReference"])(generateIngredientsList, "405410787db2d797b9d5d69a0631fed08a4836012e", null);
}),
"[project]/src/lib/aiMenuExtractor.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "extractMenuFromPdf",
    ()=>extractMenuFromPdf,
    "extractMenuItemsFromPdfWithGemini",
    ()=>extractMenuItemsFromPdfWithGemini
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$genkit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/ai/genkit.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$jsonrepair$2f$lib$2f$esm$2f$regular$2f$jsonrepair$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/jsonrepair/lib/esm/regular/jsonrepair.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$inventoryService$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/inventoryService.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$menuService$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/menuService.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$generate$2d$ingredients$2d$list$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/ai/flows/generate-ingredients-list.ts [app-route] (ecmascript)");
;
;
;
;
;
/**
 * Last-ditch recovery for broken, truncated Gemini arrays.
 * Cuts off after last valid object and closes with a bracket.
 */ function recoverLongestValidArray(jsonString) {
    console.log('🔧 Attempting array recovery for truncated JSON...');
    // Remove any trailing commas
    jsonString = jsonString.replace(/,\s*([\]}])/g, '$1');
    // Find the start of the array
    let arrStart = jsonString.indexOf('[');
    if (arrStart === -1) {
        console.log('❌ No opening bracket found');
        return '[]';
    }
    // Find all complete objects by looking for closing braces
    const completeObjects = [];
    let currentPos = arrStart + 1;
    let braceCount = 0;
    let currentObject = '';
    let inString = false;
    let escapeNext = false;
    for(let i = currentPos; i < jsonString.length; i++){
        const char = jsonString[i];
        if (escapeNext) {
            escapeNext = false;
            currentObject += char;
            continue;
        }
        if (char === '\\') {
            escapeNext = true;
            currentObject += char;
            continue;
        }
        if (char === '"' && !escapeNext) {
            inString = !inString;
            currentObject += char;
            continue;
        }
        if (inString) {
            currentObject += char;
            continue;
        }
        if (char === '{') {
            braceCount++;
            currentObject += char;
        } else if (char === '}') {
            braceCount--;
            currentObject += char;
            if (braceCount === 0) {
                // We have a complete object
                const trimmed = currentObject.trim();
                if (trimmed.length > 0) {
                    completeObjects.push(trimmed);
                    console.log(`✅ Found complete object ${completeObjects.length}`);
                }
                currentObject = '';
                // Skip any whitespace and commas
                while(i + 1 < jsonString.length && /[\s,]/.test(jsonString[i + 1])){
                    i++;
                }
            }
        } else if (braceCount > 0) {
            currentObject += char;
        }
    }
    if (completeObjects.length === 0) {
        console.log('❌ No complete objects found');
        return '[]';
    }
    const recovered = '[' + completeObjects.join(',') + ']';
    console.log(`✅ Recovered ${completeObjects.length} complete objects`);
    return recovered;
}
/**
 * Helper for batching with concurrency limit
 */ async function batchWithConcurrencyLimit(items, fn, limit) {
    const results = [];
    let idx = 0;
    const executing = [];
    async function run(item, i) {
        results[i] = await fn(item);
    }
    while(idx < items.length){
        const i = idx;
        const p = run(items[i], i);
        executing.push(p);
        idx++;
        if (executing.length >= limit) {
            await Promise.race(executing);
            // Remove finished
            for(let j = executing.length - 1; j >= 0; j--){
                if (typeof executing[j]?.then === 'function') {
                    executing.splice(j, 1);
                }
            }
        }
    }
    await Promise.all(executing);
    return results;
}
async function extractMenuFromPdf({ pdfDataUri, userId, numberOfServings = 1, skipIngredients = false }) {
    const prompt = `
You are an expert at reading restaurant menu PDFs in any language (e.g., German, English, French, etc.). Extract every dish, drink, or menu item as a JSON object.

For each menu item, extract:
- "name": Dish or beverage name, exactly as printed, but **never include the price or currency in the name**. The name must NOT contain any price or currency. Only the actual name, in the original language.
- "price": The price string for the item (e.g., "3,20 €", "2.50 EUR", "$4.00"). If no price, use an empty string.
- "category": Section/category name (e.g., "Pizza", "Biere", "Salate", "Getränke (Drinks)"). If not shown, use "Other". Always preserve the original language.
- "ingredients": Array of ingredient names if shown in menu. If not listed, use [].

**If a menu item has multiple variants (e.g., sizes or volumes with different prices), group all variants under a single menu item object. Add a 'sizes' array: each entry should have 'size' (exactly as shown in the menu, e.g., '0.3 l') and 'price'. Do not convert, round, or change the size string.**

**Return only a single valid JSON array of objects.** No explanations, no markdown, no text before or after the array.

Example output:
[
  {"name":"Coca-Cola","category":"Soft Drinks","sizes":[{"size":"0.3 l","price":"2,00 €"},{"size":"0.5 l","price":"3,00 €"},{"size":"1.0 l","price":"5,00 €"}],"ingredients":[]},
  {"name":"Zwiefalter-Engele, helles Bier","price":"3,20 €","category":"Biere","ingredients":[], "size": "0,33l"},
  {"name":"Zwiefalter-Urweizen Dunkel","price":"4,20 €","category":"Biere","ingredients":[], "size": "0,5l"},
  {"name":"Water Bottle","price":"2,00 €","category":"Getränke (Drinks)","ingredients":[], "size": "0,3l"},
  {"name":"Water Bottle","price":"3,00 €","category":"Getränke (Drinks)","ingredients":[], "size": "0,5l"},
  {"name":"Chicken Curry","price":"10,00 €","category":"Spezialitäten vom Huhn mit Reis","ingredients":[]},
  {"name":"Chicken Korma","price":"10,00 €","category":"Spezialitäten vom Huhn mit Reis","ingredients":["Mandeln", "Sahne", "Kokosnuss", "feinen Gewürzen"]},
  {"name":"Glas Prosecco 15% vol","price":"5,90 €","category":"Aperitifs","ingredients":[], "size": "0,2l"},
  {"name":"Indischer Weißwein","price":"6,90 €","category":"Indische Weine","ingredients":[], "size": "0,2l"},
  {"name":"Indischer Weißwein","price":"21,00 €","category":"Indische Weine","ingredients":[], "size": "0,75l"}
]

// BAD EXAMPLE (do NOT do this!):
// {"name": "Water Bottle (0,3l) 2,00 € (0,5l) 3,00 €", "price": "", ...}
`;
    // Call Gemini via Genkit
    const result = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$genkit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ai"].generate([
        {
            text: prompt
        },
        {
            media: {
                url: pdfDataUri
            }
        }
    ]);
    console.log('GENKIT RAW RESULT type:', typeof result);
    console.log('GENKIT RAW RESULT keys:', result ? Object.keys(result) : 'null');
    console.log('GENKIT RAW RESULT text length:', result?.text ? result.text.length : 0);
    // Extract text, from code block or raw
    let text = typeof result.text === 'string' ? result.text : '';
    let cleanText = '';
    const jsonMatch = text.match(/```json([\s\S]*?)```/) || text.match(/```([\s\S]*?)```/);
    if (jsonMatch) {
        cleanText = jsonMatch[1].trim();
    } else {
        cleanText = text.trim();
    }
    // Remove trailing code fences and comments, just in case
    cleanText = cleanText.replace(/```/g, '').replace(/^\s*\/\/.*$/gm, '').trim();
    // IMPORTANT: Remove the "json" prefix that Gemini sometimes adds
    if (cleanText.startsWith('json\n')) {
        console.log('🔧 Removing "json" prefix from response');
        cleanText = cleanText.substring(5).trim();
    } else if (cleanText.startsWith('json ')) {
        console.log('🔧 Removing "json " prefix from response');
        cleanText = cleanText.substring(5).trim();
    }
    // DEBUG: See what we are trying to parse (first 500 chars only)
    console.log('CLEAN JSON TO PARSE (first 500 chars):', cleanText.substring(0, 500));
    console.log('CLEAN JSON TO PARSE length:', cleanText.length);
    // Check if it looks like a valid JSON start
    if (!cleanText.startsWith('[') && !cleanText.startsWith('{')) {
        console.log('⚠️ Text does not start with JSON bracket, full text (first 1000 chars):', cleanText.substring(0, 1000));
    }
    let items = [];
    let parseSuccess = false;
    try {
        // 1. Try straight parse
        items = JSON.parse(cleanText);
        console.log('✅ Successfully parsed JSON directly, items count:', Array.isArray(items) ? items.length : 'not an array');
        parseSuccess = true;
    } catch (e) {
        console.log('❌ Direct JSON parse failed:', e instanceof Error ? e.message.substring(0, 200) : 'unknown error');
        // 2. Try jsonrepair
        try {
            const repairedJson = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$jsonrepair$2f$lib$2f$esm$2f$regular$2f$jsonrepair$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["jsonrepair"])(cleanText);
            console.log('🔧 JSON repair attempted, repaired length:', repairedJson.length);
            console.log('🔧 Repaired JSON (first 200 chars):', repairedJson.substring(0, 200));
            items = JSON.parse(repairedJson);
            console.log('✅ Successfully parsed repaired JSON, items count:', Array.isArray(items) ? items.length : 'not an array');
            parseSuccess = true;
        } catch (e2) {
            console.log('❌ JSON repair also failed:', e2 instanceof Error ? e2.message.substring(0, 200) : 'unknown error');
            // 3. Try best-effort recovery for truncation
            try {
                const partial = recoverLongestValidArray(cleanText);
                console.log('🔧 Array recovery attempted, recovered length:', partial.length);
                console.log('🔧 Recovered JSON (first 200 chars):', partial.substring(0, 200));
                items = JSON.parse(partial);
                console.log('✅ Successfully parsed recovered array, items count:', Array.isArray(items) ? items.length : 'not an array');
                parseSuccess = true;
            } catch (e3) {
                console.log('❌ Array recovery also failed:', e3 instanceof Error ? e3.message.substring(0, 200) : 'unknown error');
                // 4. Give up, show nothing
                items = [];
            }
        }
    }
    // Log what we actually got
    if (parseSuccess) {
        console.log('🎉 JSON parsing succeeded!');
        if (Array.isArray(items)) {
            console.log(`📊 Parsed array with ${items.length} items`);
            if (items.length > 0) {
                console.log('🔍 First item sample:', JSON.stringify(items[0]).substring(0, 100));
                // Check if this is the ["json", actual_menu_array] structure
                if (items.length === 2 && items[0] === 'json' && Array.isArray(items[1])) {
                    console.log('🔧 Detected ["json", menu_array] structure, extracting menu from second element');
                    items = items[1];
                    console.log(`✅ Extracted actual menu array with ${items.length} items`);
                }
            }
        } else {
            console.log('⚠️ Parsed successfully but result is not an array, type:', typeof items);
            console.log('🔍 Result sample:', JSON.stringify(items, null, 2).substring(0, 500));
        }
    } else {
        console.log('💥 All JSON parsing attempts failed');
    }
    // Post-process: If a single item contains multiple variants in price or name, split them into separate items
    const variantRegex = /([0-9]+[\.,][0-9]+\s*(l|cl|ml|g|kg|L|CL|ML|G|KG)?)[^\d]*(\d+[\.,]\d+\s*(€|EUR|USD|GBP|\$))/gi;
    let expanded = [];
    for (const item of items){
        if (item && typeof item.price === 'string' && variantRegex.test(item.price)) {
            // e.g. price: "(0,3l) 2,00 € (0,5l) 3,00 €"
            const matches = [
                ...item.price.matchAll(variantRegex)
            ];
            if (matches.length > 1) {
                for (const match of matches){
                    expanded.push({
                        ...item,
                        price: match[3].trim(),
                        size: match[1].trim()
                    });
                }
                continue;
            }
        }
        expanded.push(item);
    }
    // Post-process: If price is missing but name contains a price, extract it
    const priceRegex = /[-–—]\s*(\d+[\.,]\d+)\s*(EUR|USD|GBP|€|\$)?/i;
    const processed = Array.isArray(expanded) ? expanded.map((item)=>{
        if (item && typeof item.name === 'string' && (!item.price || item.price === '')) {
            const match = item.name.match(priceRegex);
            if (match) {
                const priceStr = match[1] + (match[2] ? ' ' + match[2] : '');
                return {
                    ...item,
                    price: priceStr.trim(),
                    name: item.name.replace(priceRegex, '').trim().replace(/[-–—]\s*$/, '').trim()
                };
            }
        }
        return item;
    }) : [];
    // Only filter for valid name field
    const filtered = Array.isArray(processed) ? processed.filter((item)=>item && typeof item.name === 'string' && item.name.length > 0) : [];
    // Merge items with same name/category into a single object with sizes array
    const mergedMap = new Map();
    for (const item of filtered){
        const key = `${item.name}||${item.category}`;
        if (!mergedMap.has(key)) {
            // Start new entry
            mergedMap.set(key, {
                ...item,
                sizes: item.size || item.sizes ? [] : undefined
            });
        }
        const merged = mergedMap.get(key);
        // If item has size/price, add to sizes array
        if (item.size || item.price && item.price !== '') {
            if (!merged.sizes) merged.sizes = [];
            merged.sizes.push({
                size: item.size || item.sizes && item.sizes[0]?.size || '',
                price: item.price || item.sizes && item.sizes[0]?.price || ''
            });
        }
    }
    // Finalize merged list
    const merged = Array.from(mergedMap.values()).map((item)=>{
        if (item.sizes && item.sizes.length === 1 && (!item.sizes[0].size || item.sizes[0].size.trim() === '')) {
            // Only one size and it's empty: move price to top-level
            return {
                ...item,
                price: item.sizes[0].price,
                sizes: undefined
            };
        } else if (item.sizes && item.sizes.length > 0) {
            // Remove top-level price/size if multiple sizes
            const { price, size, ...rest } = item;
            return {
                ...rest,
                sizes: item.sizes
            };
        } else {
            // Single size or no sizes
            return item;
        }
    });
    // After merging and before returning, ensure every item has a unique id
    const mergedWithIds = merged.map((item)=>{
        if (item.id && typeof item.id === 'string' && item.id.length > 0) return item;
        // Generate a unique id from name, category, price, and size (if present)
        let id = `${item.name}__${item.category}`;
        if (item.price) id += `__${item.price}`;
        if (item.size) id += `__${item.size}`;
        if (item.sizes && Array.isArray(item.sizes) && item.sizes.length > 0) {
            id += '__' + item.sizes.map((sz)=>`${sz.size}_${sz.price}`).join('__');
        }
        return {
            ...item,
            id
        };
    });
    // Ingredient enrichment logic (only if not skipped)
    if (!skipIngredients) {
        let inventory = [];
        let menuDishes = [];
        if (userId) {
            try {
                inventory = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$inventoryService$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getInventory"])(userId);
            } catch  {}
            try {
                menuDishes = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$menuService$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getDishes"])(userId);
            } catch  {}
        }
        // Helper to find ingredient with quantity in inventory or menu
        function findIngredientWithQuantity(ingredientName) {
            // Check inventory
            if (inventory && Array.isArray(inventory)) {
                const inv = inventory.find((i)=>i.name.toLowerCase() === ingredientName.toLowerCase());
                if (inv && inv.quantity && inv.unit) {
                    return {
                        name: inv.name,
                        quantity: inv.quantity,
                        unit: inv.unit
                    };
                }
            }
            // Check menu
            if (menuDishes && Array.isArray(menuDishes)) {
                for (const dish of menuDishes){
                    if (dish.ingredients && Array.isArray(dish.ingredients)) {
                        for (const ing of dish.ingredients){
                            if (typeof ing === 'object' && ing.inventoryItemName && ing.inventoryItemName.toLowerCase() === ingredientName.toLowerCase()) {
                                return {
                                    name: ing.inventoryItemName,
                                    quantity: ing.quantityPerDish,
                                    unit: ing.unit
                                };
                            }
                        }
                    }
                }
            }
            return null;
        }
        // Collect all AI ingredient generation tasks
        const aiTasks = [];
        for(let i = 0; i < mergedWithIds.length; i++){
            const item = mergedWithIds[i];
            if (item.ingredients && Array.isArray(item.ingredients) && item.ingredients.length > 0) {
                const missing = [];
                for (const ing of item.ingredients){
                    let found = findIngredientWithQuantity(ing);
                    if (!found) missing.push(ing);
                }
                if (missing.length > 0) {
                    aiTasks.push({
                        idx: i,
                        name: item.name,
                        missing
                    });
                }
            } else if (item.name) {
                aiTasks.push({
                    idx: i,
                    name: item.name,
                    missing: []
                });
            }
        }
        // Batch AI calls for ingredient generation (concurrency limit 5)
        try {
            const aiResults = await batchWithConcurrencyLimit(aiTasks, async (task)=>{
                try {
                    return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$generate$2d$ingredients$2d$list$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["generateIngredientsList"])({
                        dishName: task.name,
                        numberOfServings
                    });
                } catch  {
                    return {
                        ingredients: []
                    };
                }
            }, 5);
            // Map AI results back to menu items
            for(let i = 0; i < aiTasks.length; i++){
                const { idx, missing } = aiTasks[i];
                const aiResult = aiResults[i];
                if (!mergedWithIds[idx].ingredients || mergedWithIds[idx].ingredients.length === 0) {
                    // No ingredients, use full AI list
                    // Normalize to { inventoryItemName, quantityPerDish, unit }
                    if (aiResult && aiResult.ingredients && Array.isArray(aiResult.ingredients)) {
                        mergedWithIds[idx].ingredients = aiResult.ingredients.map((ing)=>({
                                inventoryItemName: ing.name,
                                quantityPerDish: ing.quantity,
                                unit: ing.unit
                            }));
                    } else {
                        mergedWithIds[idx].ingredients = [];
                    }
                } else {
                    // If original ingredients are just names (strings), and AI returns objects, use AI's list directly
                    if (aiResult && aiResult.ingredients && Array.isArray(aiResult.ingredients) && mergedWithIds[idx].ingredients.every((ing)=>typeof ing === 'string')) {
                        mergedWithIds[idx].ingredients = aiResult.ingredients.map((ing)=>({
                                inventoryItemName: ing.name,
                                quantityPerDish: ing.quantity,
                                unit: ing.unit
                            }));
                    } else {
                        // Mix and match
                        for (const missingName of missing){
                            const found = aiResult && aiResult.ingredients && Array.isArray(aiResult.ingredients) ? aiResult.ingredients.find((ing)=>ing.name.toLowerCase() === missingName.toLowerCase()) : null;
                            if (found) {
                                mergedWithIds[idx].ingredients.push({
                                    inventoryItemName: found.name,
                                    quantityPerDish: found.quantity,
                                    unit: found.unit
                                });
                            }
                        }
                    }
                }
            }
        } catch (ingredientError) {
            console.warn('⚠️ Ingredient enrichment failed, continuing with basic menu items:', ingredientError);
        }
    }
    return mergedWithIds;
}
async function extractMenuItemsFromPdfWithGemini(buffer) {
    // Convert buffer to data URI
    const pdfDataUri = `data:application/pdf;base64,${buffer.toString('base64')}`;
    const items = await extractMenuFromPdf({
        pdfDataUri
    });
    return items;
}
}),
"[project]/src/app/api/uploadMenu/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "POST",
    ()=>POST,
    "runtime",
    ()=>runtime
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$aiMenuExtractor$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/aiMenuExtractor.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$generate$2d$ingredients$2d$list$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/ai/flows/generate-ingredients-list.ts [app-route] (ecmascript)");
;
;
;
;
const runtime = 'nodejs';
// Retry utilities for AI quota handling
async function delay(ms) {
    return new Promise((res)=>setTimeout(res, ms));
}
async function callWithRetry(fn, retries = 2, baseDelayMs = 5000) {
    let attempt = 0;
    let lastErr;
    while(attempt <= retries){
        try {
            return await fn();
        } catch (err) {
            lastErr = err;
            const msg = String(err?.message || err);
            const isQuota = msg.includes('429') || msg.toLowerCase().includes('quota');
            const isTransient = isQuota || msg.toLowerCase().includes('timeout');
            if (attempt === retries || !isTransient) break;
            const jitter = Math.floor(Math.random() * 1000);
            const delayMs = baseDelayMs * Math.pow(2, attempt) + jitter;
            console.log(`Retrying AI call in ${delayMs}ms (attempt ${attempt + 1}/${retries + 1}) - ${msg}`);
            await delay(delayMs);
            attempt++;
        }
    }
    throw lastErr;
}
function ensureExportsDir() {
    const exportDir = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports');
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(exportDir)) __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(exportDir, {
        recursive: true
    });
    return exportDir;
}
// Enhanced heuristic text extraction with multiple patterns
function extractHeuristicFromText(text) {
    const items = [];
    const lines = text.split('\n').filter((line)=>line.trim().length > 2);
    // Enhanced regex patterns for food detection
    const foodPatterns = [
        /^([A-Z][a-z\s&'-]+)\s*[\$£€₹]?\s*(\d+(?:\.\d{1,2})?)/,
        /([A-Z][a-z\s&'-]+)[\s\t]*(\d+(?:\.\d{1,2})?)/,
        /(.+?)\s*[\$£€₹]\s*(\d+(?:\.\d{1,2})?)/,
        /^(\d+)\.\s*([A-Za-z][a-z\s&'-]+)\s*[\$£€₹]?\s*(\d+(?:\.\d{1,2})?)/
    ];
    lines.forEach((line, idx)=>{
        line = line.trim();
        if (line.length < 3) return;
        for (const pattern of foodPatterns){
            const match = line.match(pattern);
            if (match) {
                let name = match[1] || match[2] || '';
                let price = match[2] || match[3] || '';
                // If pattern has 3 groups, price is in group 3
                if (match.length === 4) {
                    name = match[2];
                    price = match[3];
                }
                name = name.trim().replace(/[\.\d]+$/, '').trim();
                if (name.length > 2 && name.length < 50 && /[a-zA-Z]/.test(name)) {
                    items.push({
                        id: `heuristic-${Date.now()}-${idx}`,
                        name: name,
                        price: price,
                        category: 'Extracted',
                        image: '',
                        ingredients: [
                            'water',
                            'salt'
                        ],
                        extractionMethod: 'heuristic'
                    });
                    break;
                }
            }
        }
    });
    return items;
}
// Three-tier extraction strategy: AI → pdf-parse → pdfjs-dist
async function extractWithFallback(file) {
    const exportDir = ensureExportsDir();
    const logPath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(exportDir, 'extraction-debug.log');
    // Tier 1: AI extraction with retry
    try {
        const pdfUri = `data:application/pdf;base64,${file}`;
        const extracted = await callWithRetry(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$aiMenuExtractor$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["extractMenuFromPdf"])(pdfUri));
        if (extracted && Array.isArray(extracted) && extracted.length > 0) {
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 1 (AI) success: ${extracted.length} items\n`);
            return extracted;
        }
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 1 (AI) failed: ${err}\n`);
    }
    // Tier 2: pdf-parse fallback
    try {
        const pdfBuffer = Buffer.from(file, 'base64');
        const pdfParse = await __turbopack_context__.A("[project]/node_modules/pdf-parse/index.js [app-route] (ecmascript, async loader)");
        const parsed = await pdfParse.default(pdfBuffer);
        const text = parsed?.text || '';
        const extractedItems = extractHeuristicFromText(text);
        if (extractedItems.length > 0) {
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 2 (pdf-parse) success: ${extractedItems.length} items\n`);
            return extractedItems;
        }
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 2 (pdf-parse) failed: ${err}\n`);
    }
    // Tier 3: pdfjs-dist fallback (simplified for Node.js)
    try {
        // Note: pdfjs-dist has Node.js compatibility issues, so we'll skip this tier for now
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 3 (pdfjs-dist) skipped due to Node.js compatibility\n`);
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 3 (pdfjs-dist) failed: ${err}\n`);
    }
    return [];
}
async function POST(request) {
    try {
        console.log('==> /api/uploadMenu POST received');
        const body = await request.json();
        console.log('payload keys:', Object.keys(body || {}));
        const { manualMenuData, file, userId, textData } = body;
        let menuItems = [];
        if (manualMenuData && Array.isArray(manualMenuData)) {
            menuItems = manualMenuData;
        } else if (textData && typeof textData === 'string') {
            // Direct text extraction
            menuItems = extractHeuristicFromText(textData);
        } else if (file && typeof file === 'string') {
            // Three-tier extraction with retry and fallback
            menuItems = await extractWithFallback(file);
        }
        // Normalize menu items
        menuItems = menuItems.map((item, idx)=>({
                id: item.id || `menu-${Date.now()}-${idx}`,
                name: item.name || item.title || `Item ${idx + 1}`,
                price: item.price || '',
                category: item.category || 'Uncategorized',
                image: item.image || '',
                ingredients: Array.isArray(item.ingredients) ? item.ingredients : [
                    'water',
                    'salt'
                ],
                extractionMethod: item.extractionMethod || 'manual'
            }));
        // Batch ingredient enrichment for extracted items
        if (menuItems.length > 0 && menuItems.some((item)=>item.extractionMethod !== 'manual')) {
            try {
                const itemsForEnrichment = menuItems.filter((item)=>item.extractionMethod !== 'manual' && Array.isArray(item.ingredients) && item.ingredients.length <= 2);
                if (itemsForEnrichment.length > 0) {
                    const batchNames = itemsForEnrichment.map((item)=>item.name).join(', ');
                    const enrichedIngredients = await callWithRetry(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$generate$2d$ingredients$2d$list$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["generateIngredientsList"])(batchNames));
                    if (enrichedIngredients && Array.isArray(enrichedIngredients)) {
                        itemsForEnrichment.forEach((item, idx)=>{
                            if (enrichedIngredients[idx] && Array.isArray(enrichedIngredients[idx])) {
                                item.ingredients = enrichedIngredients[idx];
                            }
                        });
                    }
                }
            } catch (enrichErr) {
                console.log('Batch ingredient enrichment failed:', enrichErr);
            }
        }
        // Export to CSV with enhanced data
        let csvExported = false;
        let csvPath = '';
        try {
            const exportsDir = ensureExportsDir();
            csvPath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(exportsDir, `menu-export-${Date.now()}.csv`);
            let csvContent = 'ID,Name,Price,Category,Image,Ingredients,ExtractionMethod,IngredientCount\n';
            menuItems.forEach((item)=>{
                const ingredients = Array.isArray(item.ingredients) ? item.ingredients.join(';') : '';
                const ingredientCount = Array.isArray(item.ingredients) ? item.ingredients.length : 0;
                const escapedName = String(item.name || '').replace(/"/g, '""');
                const escapedCategory = String(item.category || '').replace(/"/g, '""');
                csvContent += `"${item.id}","${escapedName}","${item.price}","${escapedCategory}","${item.image}","${ingredients}","${item.extractionMethod}","${ingredientCount}"\n`;
            });
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(csvPath, csvContent, 'utf8');
            csvExported = true;
        } catch (csvErr) {
            console.log('CSV export failed:', csvErr);
        }
        const result = {
            success: true,
            count: menuItems.length,
            csvExported,
            csvPath,
            extractionAccuracy: menuItems.length > 0 ? 100 : 0,
            extractionTier: menuItems.length > 0 ? menuItems[0].extractionMethod === 'ai' ? 1 : 2 : 0,
            menu: menuItems
        };
        return new Response(JSON.stringify(result), {
            status: 200,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    } catch (error) {
        console.error('Upload Menu API error:', error);
        return new Response(JSON.stringify({
            success: false,
            message: error instanceof Error ? error.message : 'Internal server error'
        }), {
            status: 500,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    }
}
const runtime = 'nodejs';
// Retry utilities for AI quota handling
async function delay(ms) {
    return new Promise((res)=>setTimeout(res, ms));
}
async function callWithRetry(fn, retries = 2, baseDelayMs = 5000) {
    let attempt = 0;
    let lastErr;
    while(attempt <= retries){
        try {
            return await fn();
        } catch (err) {
            lastErr = err;
            const msg = String(err?.message || err);
            const isQuota = msg.includes('429') || msg.toLowerCase().includes('quota');
            const isTransient = isQuota || msg.toLowerCase().includes('timeout');
            if (attempt === retries || !isTransient) break;
            const jitter = Math.floor(Math.random() * 1000);
            const delayMs = baseDelayMs * Math.pow(2, attempt) + jitter;
            console.log(`Retrying AI call in ${delayMs}ms (attempt ${attempt + 1}/${retries + 1}) - ${msg}`);
            await delay(delayMs);
            attempt++;
        }
    }
    throw lastErr;
}
function ensureExportsDir() {
    const exportDir = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports');
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(exportDir)) __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(exportDir, {
        recursive: true
    });
    return exportDir;
}
// Enhanced heuristic text extraction with multiple patterns
function extractHeuristicFromText(text) {
    const items = [];
    const lines = text.split('\n').filter((line)=>line.trim().length > 2);
    // Enhanced regex patterns for food detection
    const foodPatterns = [
        /^([A-Z][a-z\s&'-]+)\s*[\$£€₹]?\s*(\d+(?:\.\d{1,2})?)/,
        /([A-Z][a-z\s&'-]+)[\s\t]*(\d+(?:\.\d{1,2})?)/,
        /(.+?)\s*[\$£€₹]\s*(\d+(?:\.\d{1,2})?)/,
        /^(\d+)\.\s*([A-Za-z][a-z\s&'-]+)\s*[\$£€₹]?\s*(\d+(?:\.\d{1,2})?)/
    ];
    lines.forEach((line, idx)=>{
        line = line.trim();
        if (line.length < 3) return;
        for (const pattern of foodPatterns){
            const match = line.match(pattern);
            if (match) {
                let name = match[1] || match[2] || '';
                let price = match[2] || match[3] || '';
                // If pattern has 3 groups, price is in group 3
                if (match.length === 4) {
                    name = match[2];
                    price = match[3];
                }
                name = name.trim().replace(/[\.\d]+$/, '').trim();
                if (name.length > 2 && name.length < 50 && /[a-zA-Z]/.test(name)) {
                    items.push({
                        id: `heuristic-${Date.now()}-${idx}`,
                        name: name,
                        price: price,
                        category: 'Extracted',
                        image: '',
                        ingredients: [
                            'water',
                            'salt'
                        ],
                        extractionMethod: 'heuristic'
                    });
                    break;
                }
            }
        }
    });
    return items;
}
// Three-tier extraction strategy: AI → pdf-parse → pdfjs-dist
async function extractWithFallback(file) {
    const exportDir = ensureExportsDir();
    const logPath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(exportDir, 'extraction-debug.log');
    // Tier 1: AI extraction with retry
    try {
        const pdfUri = `data:application/pdf;base64,${file}`;
        const extracted = await callWithRetry(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$aiMenuExtractor$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["extractMenuFromPdf"])(pdfUri));
        if (extracted && Array.isArray(extracted) && extracted.length > 0) {
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 1 (AI) success: ${extracted.length} items\n`);
            return extracted;
        }
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 1 (AI) failed: ${err}\n`);
    }
    // Tier 2: pdf-parse fallback
    try {
        const pdfBuffer = Buffer.from(file, 'base64');
        const pdfParse = await __turbopack_context__.A("[project]/node_modules/pdf-parse/index.js [app-route] (ecmascript, async loader)");
        const parsed = await pdfParse.default(pdfBuffer);
        const text = parsed?.text || '';
        const extractedItems = extractHeuristicFromText(text);
        if (extractedItems.length > 0) {
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 2 (pdf-parse) success: ${extractedItems.length} items\n`);
            return extractedItems;
        }
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 2 (pdf-parse) failed: ${err}\n`);
    }
    // Tier 3: pdfjs-dist fallback (simplified for Node.js)
    try {
        // Note: pdfjs-dist has Node.js compatibility issues, so we'll skip this tier for now
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 3 (pdfjs-dist) skipped due to Node.js compatibility\n`);
    } catch (err) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(logPath, `[${new Date().toISOString()}] Tier 3 (pdfjs-dist) failed: ${err}\n`);
    }
    return [];
}
async function POST(request) {
    try {
        console.log('==> /api/uploadMenu POST received');
        const body = await request.json();
        console.log('payload keys:', Object.keys(body || {}));
        const { manualMenuData, file, userId, textData } = body;
        let menuItems = [];
        if (manualMenuData && Array.isArray(manualMenuData)) {
            menuItems = manualMenuData;
        } else if (textData && typeof textData === 'string') {
            // Direct text extraction
            menuItems = extractHeuristicFromText(textData);
        } else if (file && typeof file === 'string') {
            // Three-tier extraction with retry and fallback
            menuItems = await extractWithFallback(file);
        }
        // Normalize menu items
        menuItems = menuItems.map((item, idx)=>({
                id: item.id || `menu-${Date.now()}-${idx}`,
                name: item.name || item.title || `Item ${idx + 1}`,
                price: item.price || '',
                category: item.category || 'Uncategorized',
                image: item.image || '',
                ingredients: Array.isArray(item.ingredients) ? item.ingredients : [
                    'water',
                    'salt'
                ],
                extractionMethod: item.extractionMethod || 'manual'
            }));
        // Batch ingredient enrichment for extracted items
        if (menuItems.length > 0 && menuItems.some((item)=>item.extractionMethod !== 'manual')) {
            try {
                const itemsForEnrichment = menuItems.filter((item)=>item.extractionMethod !== 'manual' && Array.isArray(item.ingredients) && item.ingredients.length <= 2);
                if (itemsForEnrichment.length > 0) {
                    const batchNames = itemsForEnrichment.map((item)=>item.name).join(', ');
                    const enrichedIngredients = await callWithRetry(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$ai$2f$flows$2f$generate$2d$ingredients$2d$list$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["generateIngredientsList"])(batchNames));
                    if (enrichedIngredients && Array.isArray(enrichedIngredients)) {
                        itemsForEnrichment.forEach((item, idx)=>{
                            if (enrichedIngredients[idx] && Array.isArray(enrichedIngredients[idx])) {
                                item.ingredients = enrichedIngredients[idx];
                            }
                        });
                    }
                }
            } catch (enrichErr) {
                console.log('Batch ingredient enrichment failed:', enrichErr);
            }
        }
        // Export to CSV with enhanced data
        let csvExported = false;
        let csvPath = '';
        try {
            const exportsDir = ensureExportsDir();
            csvPath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(exportsDir, `menu-export-${Date.now()}.csv`);
            let csvContent = 'ID,Name,Price,Category,Image,Ingredients,ExtractionMethod,IngredientCount\n';
            menuItems.forEach((item)=>{
                const ingredients = Array.isArray(item.ingredients) ? item.ingredients.join(';') : '';
                const ingredientCount = Array.isArray(item.ingredients) ? item.ingredients.length : 0;
                const escapedName = String(item.name || '').replace(/"/g, '""');
                const escapedCategory = String(item.category || '').replace(/"/g, '""');
                csvContent += `"${item.id}","${escapedName}","${item.price}","${escapedCategory}","${item.image}","${ingredients}","${item.extractionMethod}","${ingredientCount}"\n`;
            });
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(csvPath, csvContent, 'utf8');
            csvExported = true;
        } catch (csvErr) {
            console.log('CSV export failed:', csvErr);
        }
        const result = {
            success: true,
            count: menuItems.length,
            csvExported,
            csvPath,
            extractionAccuracy: menuItems.length > 0 ? 100 : 0,
            extractionTier: menuItems.length > 0 ? menuItems[0].extractionMethod === 'ai' ? 1 : 2 : 0,
            menu: menuItems
        };
        return new Response(JSON.stringify(result), {
            status: 200,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    } catch (error) {
        console.error('Upload Menu API error:', error);
        return new Response(JSON.stringify({
            success: false,
            message: error instanceof Error ? error.message : 'Internal server error'
        }), {
            status: 500,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    }
}
// Utils
async function delay(ms) {
    return new Promise((res)=>setTimeout(res, ms));
}
async function callWithRetry(fn, retries = 2, baseDelayMs = 5000) {
    let attempt = 0;
    let lastErr;
    while(attempt <= retries){
        try {
            return await fn();
        } catch (err) {
            lastErr = err;
            const msg = String(err?.message || err);
            const isQuota = msg.includes('429') || msg.toLowerCase().includes('quota');
            const isTransient = isQuota || msg.toLowerCase().includes('timeout');
            if (attempt === retries || !isTransient) break;
            const jitter = Math.floor(Math.random() * 1000);
            const delayMs = baseDelayMs * Math.pow(2, attempt) + jitter;
            try {
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] retrying AI in ${delayMs}ms (attempt ${attempt + 1}/${retries}) msg=${msg}\n`, 'utf8');
            } catch  {}
            await delay(delayMs);
            attempt++;
        }
    }
    throw lastErr;
}
function ensureExportsDir() {
    const exportDir = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports');
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(exportDir)) __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(exportDir, {
        recursive: true
    });
    return exportDir;
}
function exportToCSV(items) {
    const header = [
        'id',
        'name',
        'category',
        'price',
        'ingredients',
        'image',
        'extractionMethod'
    ];
    const escape = (v)=>'"' + String(v ?? '').replace(/"/g, '""') + '"';
    const csvRows = [
        header.join(',')
    ];
    for (const it of items){
        const row = [
            it.id,
            it.name,
            it.category,
            it.price,
            Array.isArray(it.ingredients) ? it.ingredients.join('; ') : it.ingredients,
            it.image || '',
            it.extractionMethod || ''
        ];
        csvRows.push(row.map(escape).join(','));
    }
    return csvRows.join('\n');
}
async function saveCSVFile(content, filename) {
    const dir = ensureExportsDir();
    const full = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(dir, filename);
    await __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].promises.writeFile(full, content, 'utf8');
    return full;
}
async function processBatch(items, batchSize, processor, delayMs = 500) {
    const results = [];
    for(let i = 0; i < items.length; i += batchSize){
        const chunk = items.slice(i, i + batchSize);
        const res = await processor(chunk);
        if (Array.isArray(res)) results.push(...res);
        await delay(delayMs);
    }
    return results;
}
function getFallbackIngredients(name, category) {
    const lower = (name || '').toLowerCase();
    if (lower.includes('chicken')) return [
        'chicken',
        'salt',
        'pepper'
    ];
    if (lower.includes('rice') || lower.includes('biryani')) return [
        'rice',
        'onion',
        'spices'
    ];
    if (lower.includes('dal') || lower.includes('lentil')) return [
        'lentils',
        'turmeric',
        'salt'
    ];
    if (lower.includes('soup')) return [
        'water',
        'salt',
        'vegetables'
    ];
    if (category && category.toLowerCase().includes('dessert')) return [
        'sugar',
        'milk'
    ];
    return [
        'water',
        'salt'
    ];
}
function calculateAccuracy(items) {
    if (!Array.isArray(items) || items.length === 0) return 0;
    const score = items.reduce((acc, it)=>acc + (Array.isArray(it.ingredients) && it.ingredients.length > 0 ? 1 : 0), 0);
    return Math.round(score / items.length * 100);
}
function extractHeuristicFromText(text) {
    const lines = text.split(/\r?\n/).map((s)=>s.trim()).filter(Boolean);
    const priceRe = /(\d+[\.,]\d{1,2})(\s*(€|EUR|USD|\$|GBP))?/;
    const extractedItems = [];
    for (const line of lines){
        const m = line.match(priceRe);
        if (m) {
            const price = m[0].trim();
            let name = line.replace(priceRe, '').replace(/[\.]{2,}.*$/, '').replace(/[-–—]+\s*$/, '').trim();
            if (name && name.length > 1) {
                extractedItems.push({
                    id: `menu-${Date.now()}-${extractedItems.length}`,
                    name,
                    price,
                    category: 'Uncategorized',
                    image: '',
                    ingredients: [],
                    extractionMethod: 'text-heuristic'
                });
            }
        }
    }
    return extractedItems;
}
// Startup probe
try {
    const startupDir = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports');
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(startupDir)) __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(startupDir, {
        recursive: true
    });
    __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(startupDir, 'uploadMenu-startup.log'), `[${new Date().toISOString()}] route.ts loaded\n`, 'utf8');
} catch  {}
async function POST(req) {
    try {
        const rawText = await req.text().catch(()=>'');
        let body = {};
        try {
            body = rawText ? JSON.parse(rawText) : {};
        } catch (parseErr) {
            try {
                const dbgDir = ensureExportsDir();
                const dbgFile = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(dbgDir, 'uploadMenu-raw-request.log');
                const snippet = rawText.substring(0, 2000);
                const entry = `[${new Date().toISOString()}] parseError=${String(parseErr?.message || '')} rawLength=${rawText.length} snippet=${snippet}\n`;
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(dbgFile, entry, 'utf8');
            } catch  {}
            body = {};
        }
        console.log('==> /api/uploadMenu POST received');
        try {
            console.log('payload keys:', Object.keys(body || {}));
        } catch  {}
        try {
            const tempFile = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(os.tmpdir(), `ioms-uploadMenu-${Date.now()}.json`);
            const payloadForFile = {
                timestamp: new Date().toISOString(),
                keys: Object.keys(body || {}),
                rawLength: rawText.length,
                sample: rawText.substring(0, 2000)
            };
            try {
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(tempFile, JSON.stringify(payloadForFile, null, 2), 'utf8');
            } catch  {}
            try {
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-raw-request.log'), JSON.stringify(payloadForFile).substring(0, 2000) + '\n', 'utf8');
            } catch  {}
            try {
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-last-temp.log'), tempFile, 'utf8');
            } catch  {}
        } catch  {}
        const { manualMenuData, pdfDataUri, file, userId } = body;
        let menuItems = [];
        // Build Data URI from various payload shapes
        let pdfUri = pdfDataUri;
        if (!pdfUri && file) {
            if (typeof file === 'string' && file.startsWith('data:application/pdf')) {
                pdfUri = file;
            } else if (typeof file === 'string') {
                pdfUri = `data:application/pdf;base64,${file}`;
            } else if (typeof file === 'object' && file !== null) {
                const candidate = file.data || file.base64 || file.b64;
                if (candidate && typeof candidate === 'string') pdfUri = candidate.startsWith('data:') ? candidate : `data:application/pdf;base64,${candidate}`;
            }
        }
        try {
            const dbgDir = ensureExportsDir();
            const dbgFile = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(dbgDir, 'uploadMenu-debug.log');
            const entry = `[${new Date().toISOString()}] foundPdf=${!!pdfUri} keys=${JSON.stringify(Object.keys(body || {}))} userId=${String(userId || '')}\n`;
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(dbgFile, entry, 'utf8');
        } catch  {}
        if (manualMenuData && Array.isArray(manualMenuData)) {
            menuItems = manualMenuData;
        } else if (pdfUri) {
            // Step 1: pdf-parse first
            try {
                const base64Part = pdfUri.includes('base64,') ? pdfUri.split('base64,')[1] : pdfUri;
                const pdfBuffer = Buffer.from(base64Part || '', 'base64');
                const pdfParse = await __turbopack_context__.A("[project]/node_modules/pdf-parse/index.js [app-route] (ecmascript, async loader)");
                const parsed = await pdfParse.default(pdfBuffer);
                const text = parsed?.text || '';
                const extractedItems = extractHeuristicFromText(text);
                if (extractedItems.length > 0) {
                    menuItems = extractedItems;
                    try {
                        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] pdf-parse primary found ${extractedItems.length} items\n`, 'utf8');
                    } catch  {}
                }
            } catch (pfErr) {
                try {
                    __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] pdf-parse primary failed: ${String(pfErr?.message || pfErr)}\n`, 'utf8');
                } catch  {}
            }
            // Step 2: AI with retry/backoff if still empty
            if (!menuItems || menuItems.length === 0) {
                try {
                    const extracted = await callWithRetry(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$aiMenuExtractor$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["extractMenuFromPdf"])({
                            pdfDataUri: pdfUri,
                            userId,
                            skipIngredients: true
                        }), 2, 5000);
                    menuItems = Array.isArray(extracted) ? extracted : [];
                } catch (e) {
                    console.warn('AI extraction failed after retries, will try pdfjs fallback if available', e);
                    menuItems = [];
                }
            }
            // Step 3: pdfjs-dist fallback if still empty (with proper Node.js setup)
            if ((!menuItems || menuItems.length === 0) && pdfUri) {
                try {
                    const base64Part2 = pdfUri.includes('base64,') ? pdfUri.split('base64,')[1] : pdfUri;
                    const pdfBuffer2 = Buffer.from(base64Part2 || '', 'base64');
                    // Use pdfjs-dist for Node.js environments - try legacy first
                    let pdfjs;
                    try {
                        pdfjs = await __turbopack_context__.A("[project]/node_modules/pdfjs-dist/legacy/build/pdf.mjs [app-route] (ecmascript, async loader)");
                    } catch  {
                        // Fallback to regular build
                        pdfjs = await __turbopack_context__.A("[project]/node_modules/pdfjs-dist/build/pdf.mjs [app-route] (ecmascript, async loader)");
                    }
                    // Disable worker for server-side
                    pdfjs.GlobalWorkerOptions.workerSrc = null;
                    const loadingTask = pdfjs.getDocument({
                        data: new Uint8Array(pdfBuffer2),
                        useSystemFonts: true
                    });
                    const doc = await loadingTask.promise;
                    let fullText = '';
                    for(let p = 1; p <= doc.numPages; p++){
                        const page = await doc.getPage(p);
                        const content = await page.getTextContent();
                        const strings = content.items.map((it)=>it.str || '').join(' ');
                        fullText += '\n' + strings;
                    }
                    const extracted2 = extractHeuristicFromText(fullText);
                    if (extracted2.length > 0) {
                        menuItems = extracted2;
                        try {
                            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] pdfjs fallback found ${extracted2.length} items\n`, 'utf8');
                        } catch  {}
                    } else {
                        try {
                            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] pdfjs fallback found 0 items\n`, 'utf8');
                        } catch  {}
                    }
                } catch (e2) {
                    try {
                        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] pdfjs fallback failed: ${String(e2?.message || e2)}\n`, 'utf8');
                    } catch  {}
                }
            }
        } else {
            const details = {
                success: false,
                message: 'No menu data provided',
                receivedKeys: Object.keys(body || {}),
                rawLength: typeof rawText === 'string' ? rawText.length : 0
            };
            console.warn('uploadMenu missing pdf/manual data:', details);
            try {
                const dbgDir = ensureExportsDir();
                const dbgFile = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(dbgDir, 'uploadMenu-debug.log');
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(dbgFile, `[${new Date().toISOString()}] missingData ${JSON.stringify(details)}\n`, 'utf8');
            } catch  {}
            return new Response(JSON.stringify(details), {
                status: 400,
                headers: {
                    'Content-Type': 'application/json'
                }
            });
        }
        // Normalize
        menuItems = (menuItems || []).map((it, idx)=>({
                id: it.id || `menu-${Date.now()}-${idx}`,
                name: it.name || it.title || `Item ${idx + 1}`,
                price: it.price || '',
                category: it.category || 'Uncategorized',
                image: it.image || '',
                ingredients: Array.isArray(it.ingredients) ? it.ingredients : [],
                extractionMethod: it.extractionMethod || (pdfUri ? 'ai' : 'manual')
            }));
        // Enrich empty ingredients in batches
        const needEnrichment = menuItems.filter((x)=>!Array.isArray(x.ingredients) || x.ingredients.length === 0);
        if (needEnrichment.length > 0) {
            try {
                const { generateIngredientsList } = await __turbopack_context__.A("[project]/src/ai/flows/generate-ingredients-list.ts [app-route] (ecmascript, async loader)");
                const batchSize = 5;
                const enriched = await processBatch(needEnrichment, batchSize, async (chunk)=>{
                    const results = [];
                    for (const item of chunk){
                        try {
                            const out = await callWithRetry(()=>generateIngredientsList({
                                    dishName: item.name,
                                    numberOfServings: 1
                                }), 2, 4000);
                            const list = Array.isArray(out?.ingredients) ? out.ingredients : [];
                            results.push({
                                id: item.id,
                                ok: true,
                                ingredients: list
                            });
                        } catch (err) {
                            const fallback = getFallbackIngredients(item.name, item.category);
                            results.push({
                                id: item.id,
                                ok: false,
                                ingredients: fallback,
                                error: String(err?.message || err)
                            });
                        }
                    }
                    return results;
                }, 750);
                const byId = Object.create(null);
                for (const r of enriched)byId[r.id] = r;
                menuItems = menuItems.map((it)=>{
                    const upd = byId[it.id];
                    if (upd && Array.isArray(upd.ingredients) && upd.ingredients.length) return {
                        ...it,
                        ingredients: upd.ingredients
                    };
                    return it;
                });
            } catch (e) {
                try {
                    __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].appendFileSync(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), 'exports', 'uploadMenu-debug.log'), `[${new Date().toISOString()}] enrichment failed, using heuristics: ${String(e?.message || e)}\n`, 'utf8');
                } catch  {}
                menuItems = menuItems.map((it)=>{
                    if (!Array.isArray(it.ingredients) || it.ingredients.length === 0) return {
                        ...it,
                        ingredients: getFallbackIngredients(it.name, it.category)
                    };
                    return it;
                });
            }
        }
        // Export CSV
        const csv = exportToCSV(menuItems);
        const fileName = `menu-export-${Date.now()}.csv`;
        const csvPath = await saveCSVFile(csv, fileName).catch(()=>fileName);
        const accuracy = calculateAccuracy(menuItems);
        const resp = {
            success: true,
            count: menuItems.length,
            csvExported: true,
            csvPath,
            extractionAccuracy: accuracy,
            menu: menuItems
        };
        return new Response(JSON.stringify(resp), {
            status: 200,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    } catch (err) {
        console.error('uploadMenu fatal error:', err);
        return new Response(JSON.stringify({
            success: false,
            message: 'Internal Server Error',
            error: String(err?.message || err)
        }), {
            status: 500,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__d7a7f7ff._.js.map